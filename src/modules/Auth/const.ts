import keyMirror = require('keymirror');

const actionTypes = keyMirror({
  SET_AUTH_TOKEN: null,
  SET_BACK_URL: null,
});

const paths = {
};

const apiPaths = {
};

export {
  actionTypes,
  apiPaths,
  paths,
};
